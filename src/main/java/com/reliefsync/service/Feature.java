package com.reliefsync.service;

/** Application features that can be granted to roles. */
public enum Feature {
    DASHBOARD, MASTER_DATA, INVENTORY, REQUESTS, VERIFY, ALLOCATE, TRANSPORT, VEHICLES, REPORTS
}
